'''
Purpose: Manages expense data — adding and loading from CSV
    
    Includes:
    add_expense(): Takes user input and appends a new row to the CSV

    load_expenses(): Reads all expenses into a pandas DataFrame

    Input validation (optional, or done via utils.py)

    Optional: edit/delete expense functions in future
    
'''